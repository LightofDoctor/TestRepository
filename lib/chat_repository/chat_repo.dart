import 'models.dart';

class ChatRepository {
  List<ChatModel> getAllChats(){
    return
        ChatModel(
         UserModel(
           'Michal Grant',

         ),
        lastMessage,
        createdTime)
  }
}